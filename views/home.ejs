<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Litera</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<form action="/usersNPosts" method="post">
  <input type="text" name="title" id="title" placeholder="Search by title">

  <button type="submit">
      Search
  </button>
</form>

<body class="bg-gray-50 text-gray-800">
  <nav class="bg-white shadow-md sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 py-4 flex justify-between items-center">
      <h1 class="text-2xl font-bold">Litera</h1>
      <div class="space-x-4">
        <a href="/" class="text-sm text-gray-700 hover:text-blue-600">Home</a>
        <a href="/logout" class="text-sm text-red-500 hover:underline">Logout</a>
      </div>
    </div>
  </nav>

  <header class="max-w-7xl mx-auto px-4 py-10 text-center">
    <h2 class="text-3xl font-semibold mb-2">Discover Thoughtful Reads</h2>
    <p class="text-gray-600">Explore reviews and curated recommendations from Litera readers</p>
  </header>

  <main class="max-w-7xl mx-auto px-4 py-6 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    <% books.forEach(book => { %>
      <div class="bg-white rounded-2xl shadow-md p-5 hover:shadow-lg transition">
        <h3 class="text-xl font-semibold mb-1"><%= book.title %></h3>
        <p class="text-sm text-gray-500 mb-3">by <%= book.author || 'Unknown Author' %></p>

        <p class="text-gray-400 text-sm italic">No reviews yet.</p>

        <div class="mt-4 text-right">
          <a href="/books/<%= book.id %>" class="text-blue-500 text-sm hover:underline">Read more</a>
        </div>
      </div>
    <% }) %>
  </main>

  <footer class="text-center text-sm text-gray-500 py-6">
    &copy; <%= new Date().getFullYear() %> Litera. All rights reserved.
  </footer>
</body>
</html>
